package com.example.demo.entity;

/**
 * Enumeration representing user roles in the platform.
 * Only two roles exist: ADMINISTRATOR and STUDENT.
 */
public enum Role {
    ADMINISTRATOR,
    STUDENT
}
